#ifndef _TRACKS_H_
#define _TRACKS_H_

#include <ap_int.h>
#define N 15

typedef ap_uint<4> dint16_t;

void tracks(dint16_t A[N], dint16_t B[N], dint16_t C[N]);

//very very simple header file, to edit later when we have code built up

#endif
